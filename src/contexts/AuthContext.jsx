import { useEffect } from "react"

const AuthContext = () => {

    const API_KEY = ''

useEffect(() => {
  first

  return () => {
    second
  }
}, [API_KEY])


  return (
    <div>AuthContext</div>
  )
}

export default AuthContext